{
  "_comment": "The checker's copy of the acceptance criteria. These criteria are public: they live in the approved spec the agent works from. What the agent cannot do is run this checker, edit it, or self-attest past it. It submits an evidence file plus artifacts; the gate validates that submission after the fact.",
  "required": [
    {
      "field": "test_output",
      "type": "artifact",
      "must_contain": "test_no_retry_on_4xx"
    },
    {
      "field": "max_retries",
      "type": "integer",
      "max": 10
    }
  ]
}
